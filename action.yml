name: 'Generate .npmrc for Private Repo'
description: 'Generate .npmrc for Private Repo'
branding:
  color: 'green'
  icon: 'box'
inputs:
  org_name:
    description: 'Github repository name'
    required: true
  directory:
    description: 'directory to create npmrc'
    required: false
  auth_token:
    description: 'AuthToken for github'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
